#include<stdio.h>

int main(){
    // int a; int b=a;---->valid

    // int v = 3^3;
    // printf("%d", v );---->valid because it will give us the 0 result, but actually the expected answer is 27 and yto run exponentiation in c program we need to put (pow) function.
// char dt ="21 Dec 2020"; --->it will give us an error .

    return 0;
}